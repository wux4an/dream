defmodule CucumberTest do
  use ExUnit.Case, async: false

  @moduletag :cucumber

  test "run cucumber features" do
    # This test will be replaced by auto-generated tests from features
    # when Cucumber.compile_features!() is called in test_helper.exs
    assert true
  end
end
